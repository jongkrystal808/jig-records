# 色彩規範｜E化治具清單系統

這份文件定義系統中所有顏色的**語意**與**使用邊界**。任何新頁面、新元件需要上色時，先確認「我要表達的是狀態好壞、還是動作方向？」，再到對應的色軸挑色，不要混用、不要自創第三套。

---

## 核心原則

> 同一個顏色在全站只能代表一種語意。
> 不同語意的資訊，永遠使用不同的色軸，即使視覺上要做到「同樣搶眼」。

目前系統有兩條獨立色軸：

| 色軸 | 回答的問題 | 適用情境 |
|---|---|---|
| **狀態色軸** | 這個數值/這件事，好還是不好？ | 庫存水位、瓶頸判定、表單驗證結果、收退料記錄的成功/失敗 |
| **動作色軸** | 使用者正在做哪一種方向性的操作？ | 收料 vs 退料、未來任何「進 vs 出」「啟用 vs 停用」類的二元切換 |

兩條軸線互不借用。狀態色軸的橘/紅不能拿來表示「退料」這個動作本身；動作色軸的藍/紫不能拿來表示「庫存正常/異常」。

---

## 1. 狀態色軸（State Colors）

表達「好壞、健康度、是否需要關注」。已在治具查詢頁、產能管理頁使用。

| 變數 | 色相 | HEX（約） | 語意 | 使用場景範例 |
|---|---|---|---|---|
| `--green` | 綠 | `#22a96e` | 正常、健康、成功 | 庫存正常、開站數充足、表單送出成功、「正常」徽章 |
| `--green-soft` | 淡綠 | `rgba(34,169,110,0.12)` | 正常的底色/背景 | 正常狀態的卡片底色、表格行底色 |
| `--orange` | 橘 | `#e08e1f` | 警示、需留意但未失效 | 低水位、開站數偏低、機種需求接近上限 |
| `--orange-soft` | 淡橘 | `rgba(224,142,31,0.12)` | 警示的底色/背景 | 低水位卡片底色 |
| `--red` | 紅 | `#d8473f` | 危險、已失效、錯誤 | 缺料、開站數為 0、表單驗證錯誤、必填星號 |
| `--red-soft` | 淡紅 | `rgba(216,71,63,0.12)` | 危險的底色/背景 | 缺料行底色、錯誤提示框 |
| `--blue` | 藍 | `#2f6ee5` | 中性資訊、目前選中 | 選中的列表項、目前查看的治具、連結文字 |
| `--blue-soft` | 淡藍 | `rgba(47,110,229,0.10)` | 中性資訊底色 | 選中行底色、資訊提示卡 |

### 使用規則
- 數字本身好壞分級（例如最大開站數 0/1/5/260）一律走這條軸線的綠橘紅三階。
- `--blue` 在狀態色軸中代表「中性，沒有好壞判斷，只是被選中或被引用」，例如使用者點了某一個站點、目前查看的治具編號。
- 必填欄位的錯誤提示（沒填就送出）使用 `--red`／`--red-soft`，但**預設狀態（使用者還沒嘗試送出）不可使用任何狀態色**，必須是中性樣式（見下方「中性色」）。

---

## 2. 動作色軸（Action Colors）

表達「使用者正在執行哪一種方向性的操作」，與好壞無關。目前用於收料／退料的區分，未來任何「進/出」「啟用/停用」類二元切換都沿用此軸。

| 變數建議命名 | 色相 | HEX（建議） | 語意 | 使用場景範例 |
|---|---|---|---|---|
| `--action-in` | 藍（青藍） | `#2f7de0` | 「進」的動作：資料流入系統 | 收料模式：面板色條、輸入框邊框、送出按鈕、分頁選中態 |
| `--action-in-soft` | 淡藍 | `rgba(47,125,224,0.10)` | 收料模式的底色 | 收料模式面板背景暈染 |
| `--action-out` | 紫／灰藍 | `#6a5fc4` | 「出」的動作：資料流出系統 | 退料模式：面板色條、輸入框邊框、送出按鈕、分頁選中態 |
| `--action-out-soft` | 淡紫 | `rgba(106,95,196,0.10)` | 退料模式的底色 | 退料模式面板背景暈染 |

### 為什麼不用橘/紅做退料
退料是正常業務行為，不是異常或警示。若使用橘/紅，會讓使用者誤以為「退料＝有問題」，且會跟狀態色軸的「缺料＝紅」「低水位＝橘」混淆——這正是兩條軸線必須分離的原因。

### 使用規則
- 動作色只負責「我在哪個模式／哪個方向」，**不疊加任何好壞判斷**。退料模式本身不該讓人感覺緊張。
- 同一個畫面中，動作色與狀態色可以同時出現（例如退料模式下，某一筆記錄缺料用 `--red` 標示），兩者互不干擾，因為色相完全不同（紫 vs 紅一眼可辨）。
- 動作色要做到「環境級」的提示，不是只改一顆按鈕文字：面板色條／輸入框邊框／送出按鈕／分頁選中態，四處同時換色，才能讓使用者即使視線不在分頁按鈕上也能辨認目前模式。

---

## 3. 中性色（Neutral）

不表達任何好壞或方向，純粹用於排版層級。

| 變數 | 用途 |
|---|---|
| `--text` | 主要文字顏色 |
| `--muted` | 次要文字、輔助說明、佔位文字 |
| `--line` | 一般邊框 |
| `--line-strong` | 較強調的邊框（如分隔用途） |
| `--shadow` | 卡片陰影 |

### 使用規則
- 表單欄位的**預設狀態**（使用者尚未操作、尚未送出）一律使用中性色，即使該欄位是必填。必填只用一個 `--red` 小星號標示，不對整個輸入框套警示色。
- 必填提示文字（如「未填單號時不能送出」）使用 `--muted`，不使用 `--orange` 或 `--red`，因為這是恆常存在的提示，不是錯誤狀態。

---

## 4. 快速判斷表

| 我要表達… | 該用哪條軸 | 顏色 |
|---|---|---|
| 這支治具庫存夠不夠 | 狀態色 | 綠/橘/紅 |
| 這個站點能不能開站 | 狀態色 | 綠/橘/紅 |
| 使用者現在點了哪一筆 | 狀態色（中性） | 藍 |
| 使用者在收料還是退料 | 動作色 | 藍（收）/紫（退） |
| 表單驗證失敗了 | 狀態色 | 紅 |
| 表單欄位預設、還沒驗證 | 中性 | 灰階 |
| 必填星號 | 狀態色 | 紅（僅星號本身，不擴及整個欄位） |

---

## 5. 待辦：尚未指派顏色變數的情境

下次遇到以下情況時，回來這份文件補充對應列，不要在頁面上臨時決定：

- 啟用 / 停用（治具、機種）→ 預期會沿用動作色軸
- 上架 / 下架
- 新增 / 刪除（刪除是否該用狀態色的紅？需另外討論，因為刪除是動作而非狀態）
